import io from 'socket.io-client'
import React from 'react'
import { render } from 'enzyme'
import { assert } from 'chai'
import { it, describe } from 'mocha'

import Feedback from '../../client/components/Feedback'

const should = require('should')

describe('FeedbackComponent', function () {
  // Create a message object
  it('Should be able to create a feedback component', function () {
    
  })
})

// Feedback is saved with correct text, user and lecture


describe('CreateFeedback', function () {
  // Create a message object
  it('Should be able to create a feedback instance', function () {

  })
})